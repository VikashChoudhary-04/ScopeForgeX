profiles:
  fast:
    description: "FAST: httpx + subhunt + nuclei (limited)"
    enabled_stages: [1, 3, 6]

  full_safe:
    description: "Full safe workflow automation (CLI-only)"
    enabled_stages: [1, 2, 3, 4, 5, 6]
